# Tests

These lightweight tests check key analytical properties used in the manuscript:

- the exact image-charge potential vanishes on the grounded spherical boundary,
- the exact induced surface charge integrates to -1,
- the image energy and signed radial-force formulas match the manuscript equations.

Run from the repository root with:

```bash
python -m unittest discover -s tests
```
